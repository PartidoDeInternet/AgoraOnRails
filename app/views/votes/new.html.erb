<h1>New vote</h1>

Vas <%= confirmation_text(params[:value]) %> la iniciativa “<%= @proposal.title %>”

<% form_for([@proposal, @vote]) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.submit 'Confirmar' %>
  </p>
<% end %>

<%= link_to 'Back', votes_path %>