<% unless vote.explanation.blank? %>
  <% div_for vote do %>
    <p class="login">
      <%= vote.user.login %>
    <p>
    <p class="explanation">
      <%= vote.explanation %>
    </p>
    <p class="link">
      <%= link_to vote.link, vote.link %>
    </p>
  <% end %>
<% end %>
