<% @title = proposal_title %>

  <div class="proposals">
    <h1 class="proposals-heading"><%= proposal_info(:heading) %></h1>
    <h5><%= proposal_info(:description) %></h5>
    <%= render partial: 'proposal', :collection => @proposals %>
  </div>
